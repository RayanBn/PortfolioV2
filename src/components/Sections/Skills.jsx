import { SectionTitle } from "../Utils/SectionTitle";

const Skills = (props) => {
    return (
        <>
            <group {...props}>
                <SectionTitle>
                    Skills
                </SectionTitle>
            </group>
        </>
    );
};

export default Skills;