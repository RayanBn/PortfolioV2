import { SectionTitle } from "../Utils/SectionTitle";

const Contact = (props) => {
    return (
        <>
            <group {...props}>
                <SectionTitle>
                    Contact
                </SectionTitle>
            </group>
        </>
    );
};

export default Contact;