import { SectionTitle } from "../Utils/SectionTitle";

const Projects = (props) => {
    return (
        <>
            <group {...props}>
                <SectionTitle>
                    Projects
                </SectionTitle>
            </group>
        </>
    );
};

export default Projects;