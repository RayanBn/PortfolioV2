import { SectionTitle } from "../Utils/SectionTitle";

const Home = (props) => {
    return (
        <>
            <group {...props}>
                <SectionTitle>
                    Home
                </SectionTitle>
            </group>
        </>
    );
};

export default Home;